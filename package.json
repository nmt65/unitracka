{
  "name": "unitracka",
  "version": "1.0.0",
  "private": true,
  "description": "UniTracka - platforma pentru gestionarea aplicatiilor la universitati.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "build:pages": "npm run build:pages --prefix frontend",
    "check": "npm run check --prefix backend && npm run build --prefix frontend",
    "smoke": "npm run smoke --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
